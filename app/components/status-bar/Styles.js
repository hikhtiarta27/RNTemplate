import { StyleSheet } from "react-native";
import _style from '../../styles'
import _color from '../../styles/Color'

const Styles = StyleSheet.create({
  statusBarContainer: height => ({ 
    height: height,
    backgroundColor: _color.red
  }),  
})

export default Styles